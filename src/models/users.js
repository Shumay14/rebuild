"use strict";

const userSQL = require("./userSQL");

class User {
    constructor(body) {
        this.body = body;

    }

    async getProfile() {
        const client = this.body;
        try {
            c
        
        }
    }
}